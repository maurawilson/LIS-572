# Cowsay

# Install package
install.packages("cowsay")

# Load package
library("cowsay")

# Use the cowsay function say()
say("Spotify yearly wrapped could be so much cooler if they shared all of our data with us!")

# Read the documentation for the function say()
?say

# Now use the function say() from the package cowsay and:

# 1. Change the message to a short declaration
# about privacy and/or fanfiction and/or web scraping
# 2. Change the animal
# 3. Change the color of the animal

# Then take a screenshot of your artwork and share it in #cowsay-practice on Discord
say(what = "Spotify yearly wrapped could be so much cooler if they shared all of our data with us!", by = "stegosaurus", what_color = "green", by_color = "purple")
